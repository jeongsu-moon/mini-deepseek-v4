# report PDF is not redistributed — see arXiv:2606.19348
DeepSeek_V4.pdf

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# large training corpora (fetch locally; do not commit — sample.txt is the only tracked text)
corpus.txt
text8
*.corpus
# Step 2 multi-domain corpus: raw text, .bin token shards, manifest
data/
# Step 2 BPE tokenizer (regenerate via train_tokenizer.py)
tokenizer.json

# transient console logs (the structured .json logs in out/<name>/ are the real records)
out/*.log

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# local env / secrets (machine-specific PYTHONPATH, API keys, etc.)
.env
